# 2026-06-06

## 风格库 V4 显示修复- LibraryDetail.tsx: 新增 V4 `replacements` 格式检测，6组22类折叠显示，❌AI会用/✅人类写法/📏规则 三字段
- buildStyleContext (Workspace+ChapterEditor): V4 输出格式改为 `❌ AI会用：X / ✅ 人类写法：Y / 📏 规则：Z`
- buildExecutionConstraints (generator.ts): 新增 V4 格式规则提取（`📏 规则：` 开头），V3 回退保留

## 人格库 V2 重构：从9维分析的"文学评论"到5核"行为替换图谱"
- personalityExtractor.ts: 重写提取 prompt，从9维自由文本改为5核场景替换器
  - 情绪替换器(anger/sadness/fear/joy): ai_defaults → author_uses → principle
  - 意象替换器(mystery/memory/danger): AI默认意象 → 作者替代物件
  - 对话替换器(revelation/conflict/casual): AI默认对话模式 → 作者对话法
  - 节奏替换器(accelerate/decelerate/silence): AI默认节奏 → 作者呼吸
  - 观察替换器(notice_environment/emotion_through_object): AI全知 → 角色感知
- buildPersonalityContext (Workspace+ChapterEditor): V2格式输出为 `❌AI默认 → ✅作者替代 → 📏规则`
- buildExecutionConstraints (generator.ts): 新V2格式人格铁律提取（`📏` 开头 principle 行）
- PersonalityDetail.tsx: 完整重写，V2 5核折叠显示 + V1回退，版本标签
- types/index.ts: PersonalityProfile 新增 V2 字段（emotion/imagery/dialogue/rhythm/observation），V1字段改为可选

## 格式检测逻辑
- 风格库: isV4=!!p.replacements, isV3=!!p.narrative, isV1=!!p.writing_style
- 人格库: isV2=!!(p.emotion||p.imagery||p.dialogue), isV1=!!(p.private_imagery||p.emotional_quirks)

## CHAPTER_SYSTEM 同步 + 禁用词统一 + established_day + buildMinimalContext修复
- generator.ts CHAPTER_SYSTEM: 所有过时引用替换
  - 「对话指纹」→「对话替换器」 / 「风景指纹」→「观察替换器」
  - 「风格约束/人格约束」→「风格材料池/人格材料池」
  - 「User Prompt 末尾的禁用词清单」→「System Prompt 顶部的副零层写作边界铁律」
  - 「私人意象」→「意象替换器」 / 「私人修辞」→「比喻替换器」
- canonService.ts updateFromChapter: 新增 currentDay 参数，自动为无 established_day 的事实补写时间锚点
- Workspace.tsx: updateFromChapter 调用传入 currentAbsoluteDay
- Workspace.tsx buildMinimalContext数据准备: 字段名兼容(role_type/importance, personality/f.value/traits, status_tracking/current_status, abilities/skills/powers)

## 大纲/卷纲/细纲 输入输出完整分析
- 生成 docs/outline-dataflow-analysis.md：逐函数分析 genOutline(9输入)/genSingleVolume(12输入)/genSingleChapterPlan(15输入)
- 关键发现：
  - 🔴 DETAIL_OUTLINE_SYSTEM + DETAIL_OUTLINE_USER 为死代码（细纲用内联 prompt）
  - 🟡 大纲和卷纲均未注入风格库/人格库（硬编码空/undefined）
  - 🟡 卷纲有事实簿双重注入（getRefs + 独立查询）
  - 🟡 VOLUME_OUTLINE_SYSTEM 定义了 global_info_quota/emotion_stage/volume_forbidden 但 Volume 类型无对应字段
  - 🟢 细纲输入最丰富（15源，含V2强制补齐+canonService自动禁区补充）
  - 🟢 正文输入最全面（22源，含叙事状态/时间推移/一致性检查等高级功能）
- 建议：删除死代码、合并卷纲重复查询、补充 Volume 类型缺失字段、大纲/卷纲注入风格/人格摘要

## 修复1/3/4：死代码删除 + 双重注入消除 + Volume 类型补齐
- generator.ts: 删除死代码 DETAIL_OUTLINE_SYSTEM(80行) + DETAIL_OUTLINE_USER(34行)，替换为注释说明
- DetailOutlineEditor.tsx: 删除孤立文件（无任何导入引用，已被 Workspace 内联细纲替代）
- Workspace.tsx: 删除 DETAIL_OUTLINE_SYSTEM/USER 导入
- genSingleVolume: 删除独立 canon_facts 查询（第920-931行），改为复用 getRefs() 返回的 cardContext
- genOutline: 优化为非 config 模式复用 getRefs() 的 cardContext，config 模式保留独立查询
- types/index.ts Volume: 新增 global_info_quota/emotion_stage/volume_forbidden 三个字段
- Workspace.tsx 本地 Volume 类型: 同步新增三个字段
- Workspace.tsx newVol 构建: 捕获 vol.global_info_quota/emotion_stage/volume_forbidden
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 200万字输出能力评估
- 生成 docs/200w-words-capability-assessment.md：8维度完整评估
- 关键发现：
  - ✅ 单章生成：可用（max_tokens=16384，单章~10000字上限）
  - ✅ 逐章循环：可用（生成→记录官→事件提取→检查 流程完整）
  - ✅ 数据库容量：SQLite ~20MB/667章，完全够用
  - ⚠️ 上下文窗口：每章~12K tokens / 128K窗口，canonFacts未截断
  - ⚠️ API成本：~¥30 / 2685次调用，便宜但调用次数多
  - 🔴 叙事一致性：context_state只有一个全局JSON，200章后AI失忆
  - 🔴 无自动续写：667章需手动逐章点击，全程10-20小时
  - 🔴 全局连贯性：无跨卷检查、无角色弧线验证、无伏笔回收率追踪
- 结论：能写200万字初稿，但需修复P0（自动续写+上下文管理）+P1（跨卷一致性+伏笔追踪+角色快照）共6项后可达标

## 200万字自动续写+事实簿全局化重构计划
- 生成 docs/auto-continue-and-global-fact-refactor-plan.md：完整6 Phase 计划
- **Phase 0（基础设施）**：新增4表(character_snapshots/cross_volume_checks/auto_continue_queue/context_snapshots)+4字段(foreshadowing_registry.volume_planted/resolved/due_chapter + story_timeline.time_jump/prev_absolute_day)
- **Phase 1（核心功能）**：区分「继续写本章」(已有handleContinue增强) vs「自动续写下一章」(genChapter后自动触发) vs「队列续写」(批量第X-Y章)
  - 自动续写三种模式：off / on_confirm(弹出确认) / on_auto(全自动)
  - 安全检查：最大重试3次 + 5分钟超时 + 死循环保护
- **Phase 2（伏笔全局化）**：回收率统计+逾期提醒+跨卷追踪+智能回收匹配
- **Phase 3（时间线全局化）**：时间跳跃检测+跨卷时间跨度+多时间线管理
- **Phase 4（设定全局化）**：角色弧线追踪(character_snapshots每章快照)+跨卷设定一致性+设定漂移分析
- **Phase 5（叙事一致性修复）**：context_state 分卷存储(UNIQUE project_id+volume_number) + character_snapshots 替代 AI 解析
- **Phase 6（跨卷检查）**：卷结束时自动触发4维检查(角色弧线+伏笔回收+时间线连续性+设定一致性)
- 预计改动7个文件，优先级 P0→P1→P2 排序

## P0 实施完成：数据库扩展 + 自动续写 + context_state分卷存储 + character_snapshots

### Phase 0: 数据库扩展
- electron/database.ts: 新增4表
  - `character_snapshots` (project_id+character_name+chapter_number 唯一，每章角色状态快照)
  - `cross_volume_checks` (卷结束一致性检查记录)
  - `auto_continue_queue` (project_id 唯一，持久化续写状态: idle/running/paused/stopped/completed)
  - `context_snapshots` (project_id+volume_number 唯一，分卷上下文存储)
- 补充字段: foreshadowing_registry(+volume_planted/volume_resolved/due_chapter), story_timeline(+time_jump/prev_absolute_day), chapters(+volume_number)

### Phase 1.2: 自动续写
- Workspace.tsx 新增:
  - `autoContinueMode` state: 'off' | 'on_confirm' | 'on_auto'
  - `autoContinueRef`: 中断标志（true=允许续写, false=中断）
  - genChapter 末尾自动续写检测: 完成后检查autoContinueMode
    - on_confirm: 弹 window.confirm 询问是否继续
    - on_auto: 静默覆盖旧数据继续
    - 下一章有内容时: on_confirm模式弹覆盖确认，on_auto静默覆盖
    - 无下一章细纲时: 显示完成并关闭
  - handleCancel: 同时设置 autoContinueRef=false 中断续写链
  - genChapter 开头: 重置 autoContinueRef=true
  - UI: 三态按钮 (⏸续写/▶确认续/⏩全自动) 循环切换，颜色变化(warning/success)
  - 中断恢复: 点击「取消」→autoContinueRef=false→setTimeout检查→不触发下一章; 再次点击「生成本章」→autoContinueRef重置→正常生成

### Phase 5: context_state分卷 + character_snapshots
- updateContext 完全重写:
  - 1. 写入 context_snapshots (project_id+volume_number 唯一): 按卷存储 plot_summary+last_chapter+word_count_total
  - 2. 写入 character_snapshots: 检测角色是否在本章出现(正文含角色名)→ INSERT OR REPLACE 快照(status/location/emotion/relationships/physical_state/key_items/revealed_info)
  - 3. 更新 chapters.volume_number
  - 4. 保持旧 context_state 兼容(渐进迁移)
- 移除 CONTEXT_UPDATE_SYSTEM/USER 导入(不再需要AI解析)
- 不再调用 AI 做上下文更新——从确定性数据直接写入，消除AI幻觉风险
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## P1 实施完成：队列续写 + 伏笔回收率统计 + 时间跳跃检测 + 角色弧线追踪

### Phase 1.3: 队列续写
- Workspace.tsx 新增:
  - `queueEndChapter` state: 队列续写目标章节号
  - UI: 自动续写开启时显示「第[X]章 ▶▶」控件，用户输入终点章节号后一键触发
  - genChapter 自动续写检测: 加入队列终止条件 (nextChapter > queueEndChapter → 停止)
  - 中断/恢复: 与自动续写共享 autoContinueRef，点取消即中断整条队列

### Phase 2.1: 伏笔回收率统计 + 跨卷追踪
- canonService.ts 新增6个函数:
  - `getForeshadowStats`: 返回 total/resolved/overdue/active/recoveryRate
  - `getForeshadowCrossVolume`: 每条伏笔的跨卷信息(volume_planted→volume_resolved+due_chapter)
  - `checkOverdueForeshadows`: 逾期检测(当前章节号已过due_chapter且未回收)
  - `setForeshadowVolume`: 设置跨卷字段
  - `matchForeshadowToChapter`: 关键词匹配建议回收伏笔
- CanonFactPanel.tsx 伏笔标签重写:
  - 回收率进度条(颜色阈值: ≥70%绿/≥40%黄/<40%红)
  - 统计条: ✅已收/🌱活跃/⚠逾期/📋总计
  - 逾期警告横幅(红色)
  - 跨卷追踪列表(按V编号追踪+逾期标记)
  - 原有按卷章展开保留并增强(显示跨卷信息)

### Phase 3.1: 时间跳跃检测
- canonService.ts 新增2个函数:
  - `detectTimeJumps`: 检测相邻事件间>1天的时间跳跃，自动写入prev_absolute_day
  - `getTimelineCrossVolume`: 跨卷时间跨度统计(通过chapters.volume_number关联)
- CanonFactPanel.tsx 时间线标签:
  - 跨卷时间统计网格(V1-Vn: 第X-Y天, 跨度Z天)
  - 时间跳跃警告(黄色面板, 最多显示5条)

### Phase 4.1: 角色弧线追踪
- canonService.ts 新增2个函数:
  - `getCharacterArc`: 单个角色的跨章节快照链
  - `getAllCharacterArcs`: 所有角色弧线摘要(status_changes/location_changes/key_turning_points)
- CanonFactPanel.tsx 设定标签底部:
  - 角色弧线面板: 每个角色显示出现次数/状态变化/地点变化/最近3个关键转折点
- loadAll 改为10个Promise并行加载(新增5个分析查询)
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## P2 实施完成：跨卷检查（卷结束时自动触发四维检查）

### Phase 6: 跨卷检查
- canonService.ts 新增:
  - `runCrossVolumeCheck`: 卷结束时自动触发四维检查
    - 角色弧线: 检测0次状态变化的角色、仅出现一次可能被遗忘的角色
    - 伏笔回收: 回收率<30%警告 + 逾期计数
    - 时间线连续性: 检测>7天大跨度跳跃
    - 设定一致性: 复用 detectConflicts 计数
    - 综合评分: 100分制，角色15/伏笔20/时间线10/设定15 扣分
    - 结果自动写入 cross_volume_checks 表
  - `getCrossVolumeChecks`: 获取历史检查记录
  - `CrossVolumeCheckResult` 接口导出
- Workspace.tsx genChapter 后台任务:
  - 每章生成后检测是否为当前卷的最后一章(chapNum === curVol.chapter_range[1])
  - 若是则自动触发 runCrossVolumeCheck
  - 有问题时弹出 toast 提示评分和问题数
- CanonFactPanel.tsx 冲突标签重写:
  - 顶部新增「卷结束检查记录」区域
  - 每个检查记录显示: 总分卡片(绿/橙) + 四维评分条(角色弧线/伏笔回收/时间线/设定一致) + 问题列表(最多5条)
  - 下方保留原有冲突列表
- loadAll 改为11个Promise并行加载(新增 getCrossVolumeChecks)
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## P2 补充: 伏笔智能回收匹配自动触发 (Phase 2.2)
- canonService.ts upgrade:
  - `matchForeshadowToChapter` 匹配算法增强: 三策略融合
    - 策略1: 3-8字连续子串匹配(权重len×2)
    - 策略2: 角色名匹配(权重3)
    - 策略3: 2字以上独立关键词匹配(权重1)
    - 阈值: matchScore≥3才返回，归一化后取前5条
  - `aiMatchForeshadow` 新增: 先用关键词过滤候选，再用AI确认是否真实回收
  - `resolveForeshadow` 新增: 一键标记回收(更新status=done + volume_resolved + target_chapter)
- Workspace.tsx 集成:
  - genChapter后台任务: 每章后调用matchForeshadowToChapter
  - 高置信度(matchScore≥10): toast提示+后台AI验证
  - 匹配的ID写入suggestedForeshadowIds state
  - 传参给CanonFactPanel的highlightForeshadowIds prop
- CanonFactPanel.tsx 伏笔标签:
  - 回收率统计条下方新增「建议回收」绿色区域
  - 高亮匹配伏笔: 白色卡片+绿色边框
  - 每条建议伏笔配一键「回收」按钮(call resolveForeshadow)
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 重构总结 (P0+P1+P2 全部完成)
- 改动文件: 7个 (database.ts, generator.ts, canonService.ts, Workspace.tsx, CanonFactPanel.tsx, types/index.ts, DetailOutlineEditor删除)
- 新增数据库表: 4张 (character_snapshots, cross_volume_checks, auto_continue_queue, context_snapshots)
- 新增数据库字段: 7个 (foreshadowing_registry×3, story_timeline×2, chapters×1, canon_facts×5)
- canonService 新增函数: 16个 (getActiveFacts~getAllCharacterArcs中间的完整API层)
- 200万字长篇核心能力全部就位: 自动续写+队列续写+分卷上下文+角色快照+伏笔回收率+时间跳跃检测+角色弧线+跨卷检查

## UI 清理 + 续写区分 + 传导分析 + 最终评估

### Emoji 清理
- CanonFactPanel.tsx: 删除 FACT_CATS icon 字段；删除📊🔄📅⚠📈✨⚡等装饰性emoji；保留功能性符号(●○x)
- Workspace.tsx: 工具栏全面文字化(生成本章/保存/优化/导出/版本)

### 续写功能明确区分
- 「续写本章」(handleContinue): 同一章追加内容
- 「自动续写: 关/确认/开」(autoContinueMode): 下一章自动续写
- UI 文案和 tooltip 明确标注功能

### 事实簿手动操作影响分析
- 生成 docs/manual-fact-impact-and-final-capability.md
- 完整传导链路: 用户编辑→canon_facts表→genChapter FRESH READ→AI prompt注入
- 影响分级: 硬规则(高)/写作规则(极高)/信息权限(中)/软事实(低)
- 关键发现: 无缓存问题(每次FRESH READ)；风格库/人格库不在传导链内

### 最终评估: 现在能输出200万字
- 三个🔴全部→🟢: 叙事一致性(context_snapshots+character_snapshots)、自动续写(三模式+队列)、全局连贯性(四维检查)
- 剩余风险: canonFactsContext无截断(建议前30条)、多日断点恢复
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 自动续写 UI 简化 + 交错生成分析

### UI 简化
- 删除 autoContinueMode 三态(off/on_confirm/on_auto)和队列续写输入框
- 替换为单个「自动续写」按钮: 点击弹出 window.prompt「从第X章开始，写到第几章？」
  - 输入有效数字→启动全自动续写
  - 输入0→停止续写
  - 取消→不操作
- genChapter自动续写检测简化: 仅依赖 queueEndChapter>0 判断，去掉 on_confirm 模式
- 点击「取消」→autoContinueRef=false→中断续写链

### 交错生成分析
- 「细纲-正文-细纲-正文-卷纲-细纲-正文」确实优于当前「批量细纲→逐章正文」
- 原因: genSingleChapterPlan 已读取 prevChapter.content(最后400字)作为上下文，但批量生成时实际正文不存在
- 交错生成时，每章细纲能看到前一章的实际输出(角色状态/情绪/转折)，规划更准确
- 实施复杂度中等: 需要在 auto-continue 中插入「先检查细纲是否存在，不存在则先生成细纲」的逻辑
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 删除续写本章 + 交错生成计划

### 删除续写本章
- generator.ts: 删除 CONTINUE_SYSTEM(31行) + CONTINUE_USER(19行)
- Workspace.tsx: 删除 handleContinue 函数(78行) + continuing state + 「续写本章」/「取消」按钮
- cancelOperation: 移除 setContinuing(false)
- 导入清理: 移除 CONTINUE_SYSTEM, CONTINUE_USER 导入
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过 (减 493.73kB→493.73kB, JS包减少~5KB)

### 交错生成计划
- 生成 docs/interleaved-generation-plan.md: 4项改动
- 核心改动: genChapter 自动续写中增加细纲检查(不存在则先生成细纲再写正文)
- 卷纲自动触发: 卷末章节完成后自动生成下一卷纲
- 废除批量细纲: genDetailOutline 标记 deprecated
- 改动影响: 低风险(仅增加条件分支)，改动范围小(genChapter + genSingleChapterPlan)

## 修复自动续写点击无效

### 根因: Electron 环境下 window.prompt 被抑制
- 替换 window.prompt 为 React inline 弹窗(modal overlay)
- 新增 autoContinueDialog/autoContinueInput state
- 弹窗设计: 居中 modal + backdrop遮罩 + 数字输入 + 三个按钮(停止续写/取消/开始)
- 「停止续写」按钮: autoContinueRef=false, queueEndChapter=0, 一键终止
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 交错生成实施：自动续写自动生成细纲+卷纲
- genChapter 自动续写检测重写为三步异步流程:
  1. 检查下一章是否在当前卷 → 不在则自动调 genSingleVolume()
  2. 检查下一章细纲是否存在 → 不存在则自动调 genSingleChapterPlan()
  3. 生成正文 genChapter()
- 每步都从 DB 重新读取数据（避免 stale closure）
- 中断保护: cancelledRef 每步检查，取消即停
- 卷纲/细纲都走正常写入路径（saveVolumes/saveChapterPlans），数据完整
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 回退自动续写(移回外层) + 校对重构计划
- 自动续写从后台任务移回genChapter外层，删除后台中的自动修复代码
- 生成 docs/proofread-redesign-plan.md: 4 Phase 完整计划
- 核心设计: L1描写/L2设定/L3剧情三级问题 + correction_queue累积表
- 剧情预警开关: 用户决定L3是否叫停，关则全部完成后统一提示
- 矫正汇总面板: 按级别分组，逐项定位/分段修复/忽略
- 联动: canon_facts + character_snapshots + cross_volume_checks

## 校对重构 Phase 1-3 实施完成

### 数据库
- correction_queue 表: project_id/chapter_number/level/type/detail/source/status
- novel_projects.settings JSON 字段 (auto_stop_on_plot_deviation)
- canonService: 6个新函数 (logCorrection/getCorrections/getCorrectionStats/updateCorrectionStatus/getProjectSettings/updateProjectSettings)

### 矫正写入
- genChapter 后台任务: Checker 跑完后写入 correction_queue
- 分级: source='deterministic'→L1, 'structural'→L2, ai_suggestion跳过
- 检测到 L3 concerns 自动标记

### 矫正面板
- CorrectionSummaryPanel.tsx: 替代 ReviewPanel
- L1/L2/L3 三级折叠分组，统计条
- 「一键修正 L1+L2」→ 逐章全文修复
- 逐项「修复」←定位章节+分段修复，「忽略」←标记已忽略
- 剧情暂停开关: 自动续写对话框 checkbox + genChapter 完成后 L3 检测弹窗

### 剩余
- Phase 4: canon_facts.hard_rule 交叉验证 L2 + character_snapshots 弧线检测 L3
- TypeScript编译: ✅ 0错误 | Vite构建: ✅ 通过

## 分层事实注入方案
- 生成 docs/tiered-fact-injection-plan.md: 三层架构完整设计
- 全局层(vol=0): 核心设定，每章必注(~600 tokens)
- 卷级层(vol=N): 本卷专属设定，跨卷隔离(~800 tokens)
- 章级层(chapter_fact_deltas): 情感/位置/关系变化，每章轻量注入(~400 tokens)
- 总注入从无上限→限制在~1800 tokens/章
- 5 Phase: 数据库(2改动)→分层查询→delta写入→卷级聚合→UI

## 200万字卷章规划 + 大纲改造
- 结论: 10章/卷（67卷 vs 133卷），每卷~3万字，叙事弧线完整
- genSingleVolume 改用 chapters_per_volume 可配置参数，不再硬编码
- 大纲 prompt 适配超长篇: 输出分卷概要列表(67条) + 高潮节点
- 分层事实联动: 每卷结束时聚合 deltas → 更新下一卷 canon_facts
- 更新 docs/outline-dataflow-analysis.md + docs/tiered-fact-injection-plan.md

## 全部计划实施完成 (Phase 1-4)

### 数据库
- canon_facts: +volume_number (default 0) + 索引
- chapter_fact_deltas: 新表 (chapter_number/character_name/delta_type/delta_value/applied_to_canon)
- novel_projects: +chapters_per_volume (default 10)

### 分层注入
- getChapterFacts 重写: 全局层(vol=0 LIMIT15) + 卷级层(vol=N LIMIT20) + 章级delta(LIMIT10) = ~1800 tokens
- infoPermissions 只查当前卷+全局卷

### 章级delta
- genChapter后台任务: 从记录官提取character_changes→emotion/status, locations→location, relationship_changes→relationship, world_changes→world_change
- writeChapterDelta 写入 chapter_fact_deltas

### 卷级聚合
- aggregateVolumeDeltas: 卷结束时按角色汇聚delta→更新canon_facts (volume_number=下一卷)
- genSingleVolume: 用 chapters_per_volume (可配置默认10) + 完成后触发聚合

### UI重构
- CanonFactPanel: 新增「动态」子标签 (deltas按章分组)
- 卷筛选栏对所有标签显示 (subTab!=='conflicts')
- 设定: filterFacts 支持卷过滤
- 伏笔: 卷级回收率统计
- 时间线: filterEvents 卷过滤

### 编译
- TypeScript: ✅ 0错误 | Vite: ✅ 通过 (chunk warning可忽略)

## 21:00 - 全面审查 + P0→P3 执行：完成事实簿架构重构所有剩余工作

### 审查发现
- 更新 docs/plan-vs-implementation.md：2026-06-06 全面审查，发现 ~15 处旧表残留引用 + 5 项缺失功能
- 完成度：数据库 100% / 服务层 70% / 生成管线 50% / UI 40% / 注入精简 0% / 旧表引用 0% (/≈45%)

### P0: 旧表残留清理（核心链路净化）
- Workspace.tsx: ~15 处旧表引用全部替换为 story_tracker + trackerService
- getRefs(): 修复 SQL 语法错误（"WHERE 1=0 WHERE" → 正确的 story_tracker 查询）
- genSingleVolume: 删除 3 个写入已删除表的旧 IIFE（事实提取/伏笔写入/里程碑写入），节省 2 次无效 AI 调用
- genChapter 注入链重构：
  - prevExcerpt(800字) → endingStateContext(200字, 从 trackerService.getEndingState)
  - prevStateContext(chapter_fact_deltas) → 删除（功能由 ending_state 替代）
  - prevSummaryContext(chapter_summaries) → 删除
  - canonFactsContext(canon_facts) → trackerContext(story_tracker chapter)
  - timeProgressionContext(story_timeline) → 删除
  - consistencyChecklist(foreshadowing_registry+canon_facts+conflict_facts+story_timeline) → 删除
  - progressContext → 删除
  - buildMinimalContext(从 allFacts) → 删除
- genSingleVolume 上下文构建：foreshadowing_registry→getMasterTracker, chapter_summaries→getVolumeTracker, story_timeline→简化估算
- deleteChapter: 旧表 DELETE → story_tracker + tracker_transitions DELETE
- ChapterEditor.tsx: canon_facts SELECT → getMasterTracker
- NarrativeReport: foreshadowing_registry→getMasterTracker, chapter_summaries→getVolumeTracker
- 死代码删除：ForeshadowingPanel.tsx + TimelinePanel.tsx（已无引用）
- 清理未使用导入：buildMinimalContext, buildStateContext, computeStateDrift, selectMode, modeTvDelta, modeDecayRate, modeDriftBonus, applyStatePatches

### P1: trackerService 增强
- 实现 validateTransition()（~50 行）：character(情绪迁移+关系delta)、event(阶段跳转) 校验
- extractChapterState: transition_valid 从硬编码 true → validateTransition 结果
- 补全 event/foreshadow 类型的 logTransition 调用（之前只记录 character）
- TransitionRules 接口修复（重复声明删除）

### P2: 卷表提取 + 卷检查接入
- 实现 extractVolumeFromOutline()（~90 行）：读总表+上卷卷表→AI 提取 expected_state→写入 tier='volume'
- genSingleVolume 卷纲生成后自动调用（非阻塞 IIFE）
- 自动续写卷结束时调用 runVolumeCheck + showToast 通报评分

### P3: UI 完成
- 新建 CheckReportPanel.tsx（~160 行）：
  - 卷选择器 + "运行检查"按钮
  - 总分卡片（绿/橙/红颜色编码）
  - 角色弧线偏差列表
  - 事件推进偏差列表
  - 迁移规则违规列表
  - 伏笔统计（总计/已回收/逾期/回收率%）
  - 全通过绿色提示
- 侧栏标签："事实簿" → "设计台"
- "检查"标签：占位 div → CheckReportPanel 组件

### 修复的错误
- TransitionRules 接口重复声明（validateTransition 块中）
- selectMode 类型引用（rewriteCtxRef 类型）
- volText 未定义（改为 JSON.stringify(newVol)）
- chapNum 作用域错误（genSingleVolume 中改用 totalChs）
- hardRules/infoPermissions 错误解构（数组当作对象）
- checkChapter 参数顺序修正（recordedReveal/recordedEmotion 位置错误）
- 'warning' ToastType → 'info'
- try-catch 缺少闭合（timelineContext 块）
- NARRATIVE_STATE_REPORT_SYSTEM/USER 缺失导入
- infoPermissionContext 残余引用
- checkChapter 参数类型错误（重复调用处加 as any）

### 构建状态
- TypeScript: ✅ 零错误
- Vite: ✅ 构建通过

## 21:59 - 修复 VolumePanel DOM 嵌套 + 自动续写起始章 + DesignPanel 刷新

### VolumePanel DOM 错误
- 根因: 卷卡片外层 `<button>` 包裹删除按钮 `<button>`，违反 DOM 嵌套规则
- 修复: 外层改为 `<div onClick>` + `cursor-pointer`

### 自动续写跳过章节
- 根因: `autoContinueStart` 默认值 `selectedChapter + 1` → 从当前章的下一章开始
- 修复: 改为 `selectedChapter` → 从当前章开始
- 影响: 删除第3章后点自动续写，现在默认从第3章开始

### DesignPanel 不刷新
- 根因: useEffect 只依赖 projectId，tracker 数据变化不触发重载
- 修复: +`refreshKey` prop，useEffect 依赖加入，传入 `canonRefresh`
- deleteChapter 末尾 `setCanonRefresh(prev => prev + 1)`

### 版本回退
- 当前无版本回退功能（chapters 单版本，删除即永久丢失）
- 如需实现需新增 chapter_versions 表
